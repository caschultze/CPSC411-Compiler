main() {

    // assignments & expressions

    // 1. literals
    // x = 1;
    // x = "hello";
    // x = true;
    // x = false;

    // 2. unary expressions
    //x = -1;
    //x = !1;

    // 3. simple expressions
    //x = (x = 1);
    //x = 1 + 2;
    //x = 1 - 2;
    //x = 1 * 2;
    //x = 1 / 2;
    //x = 1 % 2;
    

    // 4. more complicated expressions
    //x = a * a < b && c * d != -g;

    // 3. functioninvocation
    //init(1, a, true);
    //parser();   

    //x = init(1, a, true);

    //return;
    //return x = "hello";

    //while (isspace(ch = getc()))
	//	;
    
    //while (isdigit(ch)) {
	//		attr = attr * 10 + (ch - ASCII_0);
	//		ch = getc();
	//	}



}