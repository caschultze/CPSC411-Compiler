#include "ast.hpp";

class Traversal {
    public:

    private:
        // void postorder(ASTNode* node) {
        //     if (node == nullptr) {
        //         return;
        //     }

        //     for (int i = 0; i < node->children.size(); i++) {
        //         postorder(node->children[i]);
        //     }

        // }
        // Let's make ASTNode children be a vector of pointers.
};