#ifndef TRAVERSAL_HPP
#define TRAVERSAL_HPP

class Traversal {
    public:
        Traversal() {}
        
        void preorder(ASTNode* node);
        void postorder(ASTNode* node);

    private:
};

#endif